<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nur.monglaport">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />


    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".Class.AppClass"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.Front">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.History" />
        <activity android:name=".UI.Telephone_dir" />
        <activity android:name=".UI.Containers" />
        <activity android:name=".UI.Location" />
        <activity android:name=".UI.Equipment" />
        <activity android:name=".UI.Navigation" />
        <activity android:name=".UI.Tide_schedule" />
        <activity android:name=".UI.Ship" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".UI.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Admin.Admin_login" />
        <activity android:name=".Admin.Admin_page_tide" />
        <activity android:name=".Admin.Admin_page_fpofficer" />
        <activity android:name=".Admin.Admin_page_telephone" />
        <activity android:name=".UI.focal_point_off" />
        <activity android:name=".Admin.Admin_page_fofficer_update" />
        <activity android:name=".Admin.Admin_page_teldir_update" />
        <activity android:name=".Admin.Admin_page_tide_update" />
       </application>

</manifest>